# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/userguide.html for a complete reference!

set $mod Mod1

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
font pango:monospace 8

# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
#font pango:DejaVu Sans Mono 8

# Before i3 v4.8, we used to recommend this one as the default:
# font -misc-fixed-medium-r-normal--13-120-75-75-C-70-iso10646-1
# The font above is very space-efficient, that is, it looks good, sharp and
# clear in small sizes. However, its unicode glyph coverage is limited, the old
# X core fonts rendering does not support right-to-left and this being a bitmap
# font, it doesn’t scale on retina/hidpi displays.

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal

# start a web browser
bindsym $mod+Shift+Return exec /usr/bin/sensible-browser

# kill focused window
bindsym $mod+Shift+q kill

# start dmenu (a program launcher)
bindsym $mod+d exec dmenu_run
# There also is the (new) i3-dmenu-desktop which only displays applications
# shipping a .desktop file. It is a wrapper around dmenu, so you need that
# installed.
# bindsym $mod+d exec --no-startup-id i3-dmenu-desktop

# rofi app and window launchers
bindsym $mod+b exec rofi -show drun -theme regolith-theme
bindsym $mod+Shift+b exec rofi -show run -theme regolith-theme
bindsym $mod+Ctrl+b exec rofi -show window -theme regolith-theme

# change focus
bindsym $mod+h focus left
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right

# move windows in workspaces
bindsym $mod+Shift+h move left
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right

# split in horizontal orientation
bindsym $mod+Shift+v split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
bindsym $mod+Shift+s layout stacking
bindsym $mod+Shift+w layout tabbed
bindsym $mod+Shift+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# Don't have mouse hover change the focused window
focus_follows_mouse no

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1 Browser"
set $ws2 "2 DevTools"
set $ws3 "3 NPM"
set $ws4 "4 Enterprise"
set $ws5 "5 Builder"
set $ws6 "6 MySQL"
set $ws7 "7 Zoom"
set $ws8 "8 KeeWeb"
set $ws9 "9"
set $ws10 "10"
set $wsM "Merge"

# switch to workspace
bindsym $mod+1 workspace $ws1
bindsym $mod+2 workspace $ws2
bindsym $mod+3 workspace $ws3
bindsym $mod+4 workspace $ws4
bindsym $mod+5 workspace $ws5
bindsym $mod+6 workspace $ws6
bindsym $mod+7 workspace $ws7
bindsym $mod+8 workspace $ws8
bindsym $mod+9 workspace $ws9
bindsym $mod+0 workspace $ws10
bindsym $mod+m workspace $wsM

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace $ws1
bindsym $mod+Shift+2 move container to workspace $ws2
bindsym $mod+Shift+3 move container to workspace $ws3
bindsym $mod+Shift+4 move container to workspace $ws4
bindsym $mod+Shift+5 move container to workspace $ws5
bindsym $mod+Shift+6 move container to workspace $ws6
bindsym $mod+Shift+7 move container to workspace $ws7
bindsym $mod+Shift+8 move container to workspace $ws8
bindsym $mod+Shift+9 move container to workspace $ws9
bindsym $mod+Shift+0 move container to workspace $ws10

# reload the configuration file
bindsym $mod+Shift+Ctrl+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3
bindsym $mod+Shift+Ctrl+e exit

# lock the screen
#bindsym Mod0+l exec gnome-screensaver-command --lock

# put the system to sleep
bindsym $mod+Shift+Ctrl+s exec systemctl suspend
# resize window (you can also use the mouse for that)
mode "Resize Mode" {
              # These bindings trigger as soon as you enter the resize mode
      bindsym Left resize shrink width 6 px or 6 ppt
      bindsym Down resize grow height 6 px or 6 ppt
      bindsym Up resize shrink height 6 px or 6 ppt
      bindsym Right resize grow width 6 px or 6 ppt

      bindsym Shift+Left resize shrink width 12 px or 12 ppt
      bindsym Shift+Down resize grow height 12 px or 12 ppt
      bindsym Shift+Up resize shrink height 12 px or 12 ppt
      bindsym Shift+Right resize grow width 12 px or 12 ppt

      bindsym h resize shrink width 6 px or 6 ppt
      bindsym k resize grow height 6 px or 6 ppt
      bindsym j resize shrink height 6 px or 6 ppt
      bindsym l resize grow width 6 px or 6 ppt

      bindsym Shift+h resize shrink width 12 px or 12 ppt
      bindsym Shift+k resize grow height 12 px or 12 ppt
      bindsym Shift+j resize shrink height 12 px or 12 ppt
      bindsym Shift+l resize grow width 12 px or 12 ppt

      # back to normal: Enter or Escape
      bindsym Return mode "default"
      bindsym Escape mode "default"
      bindsym $mod+r mode "default

}
bindsym $mod+r mode "Resize Mode"

bindsym $mod+Shift+Ctrl+z exec --no-startup-id killall zoom

# Disable titlebar
new_window pixel 0
new_float pixel 0
# class                 border  backgr. text    indicator child_border
client.focused          #002b36 #586e75 #fdf6e3 #268bd2
client.focused_inactive #002b36 #073642 #839496 #073642
client.unfocused        #002b36 #073642 #839496 #073642
client.urgent           #002b36 #dc322f #fdf6e3 #002b36

# Enable popup during fullscreen
popup_during_fullscreen smart

# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
bar {
      status_command i3status
}
#Start of custom


# Move the current workspace to the next output
# (effectively toggles when you only have two outputs)
bindsym $mod+x move workspace to output right
bindsym $mod+z move workspace to output left
bindsym $mod+Shift+x move workspace to output down
bindsym $mod+Shift+z move workspace to output up

#bindsym $mod+z move scratchpad
#bindsym $mod+o scratchpad show
bindsym $mod+semicolon floating toggle; resize set 1500 800; move position center

for_window [instance="gnome-terminal-server"] floating enable
for_window [instance="gnome-terminal-server"] resize set 1500 800
for_window [instance="gnome-terminal-server"] move scratchpad
for_window [instance="gnome-terminal-server"] border pixel 2
exec_always --no-startup-id tat-enterprise
bindsym $mod+t [instance="gnome-terminal-server"] scratchpad show; [instance="gnome-terminal-server"] move position center

for_window [instance="calendar.google.com"] floating enable
for_window [instance="calendar.google.com"] resize set 1500 800
for_window [instance="calendar.google.com"] move scratchpad
for_window [instance="calendar.google.com"] border pixel 2
exec_always --no-startup-id goog-cal
bindsym $mod+c [instance="calendar.google.com"] scratchpad show; [instance="calendar.google.com"] move position center

for_window [instance="mail.google.com"] floating enable
for_window [instance="mail.google.com"] resize set 1500 800
for_window [instance="mail.google.com"] move scratchpad
for_window [instance="mail.google.com"] border pixel 2
exec_always --no-startup-id goog-mail
bindsym $mod+e [instance="mail.google.com"] scratchpad show; [instance="mail.google.com"] move position center

for_window [instance="app.intercom.io"] floating enable
for_window [instance="app.intercom.io"] resize set 1500 800
for_window [instance="app.intercom.io"] move scratchpad
for_window [instance="app.intercom.io"] border pixel 2
exec_always --no-startup-id intercom-app
bindsym $mod+i [instance="app.intercom.io"] scratchpad show; [instance="app.intercom.io"] move position center

for_window [instance="slack"] floating enable
for_window [instance="slack"] resize set 1500 800
for_window [instance="slack"] move scratchpad
for_window [instance="slack"] border pixel 2
exec_always --no-startup-id slack
bindsym $mod+s [instance="slack"] scratchpad show; [instance="slack"] move position center

for_window [instance="spotify"] floating enable
for_window [instance="spotify"] resize set 1500 800
for_window [instance="spotify"] move scratchpad
for_window [instance="spotify"] border pixel 2
exec_always --no-startup-id spotify-app
bindsym $mod+p [instance="spotify"] scratchpad show; [instance="spotify"] move position center

for_window [instance="sublime_text"] floating enable
for_window [instance="sublime_text"] resize set 1500 800
for_window [instance="sublime_text"] move scratchpad
for_window [instance="sublime_text"] border pixel 2
exec_always --no-startup-id sublimetext-app
bindsym $mod+u [instance="sublime_text"] scratchpad show; [instance="sublime_text"] move position center

# for_window [instance="sublime_merge"] floating enable
# for_window [instance="sublime_merge"] resize set 1500 800
# for_window [instance="sublime_merge"] move scratchpad
# for_window [instance="sublime_merge"] border pixel 2
exec_always --no-startup-id sublimemerge-app
# bindsym $mod+m [instance="sublime_merge"] scratchpad show; [instance="sublime_merge"] move position center
for_window [instance="sublime_merge"] move container to workspace $wsM

exec_always --no-startup-id alarm-app
for_window [instance="alarm-clock-applet"] move container to workspace $ws3

#Automatically move windows to workspaces
for_window [instance="mysql-workbench-bin"] move container to workspace $ws6
for_window [instance="google-chrome" title="^DevTools"] move container to workspace $ws2

for_window [instance="zoom" title="Zoom - Free Account"] floating enable
for_window [instance="zoom" title="Zoom - Free Account"] resize set 1500 800
for_window [instance="zoom" title="Zoom - Free Account"] move scratchpad
for_window [instance="zoom" title="Zoom - Free Account"] border pixel 2
exec_always --no-startup-id sublimemerge-app
bindsym $mod+o [instance="zoom" title="Zoom - Free Account"] scratchpad show; [instance="zoom" title="Zoom - Free Account"] move position center

for_window [instance="zoom" title="^Zoom Meeting ID: "] floating disable
for_window [instance="zoom" title="^Zoom Meeting ID: "] move container to workspace $ws7;

bindsym $mod+q [instance="gnome-terminal-server"] move scratchpad; [instance="calendar.google.com"] move scratchpad; [instance="mail.google.com"] move scratchpad; [instance="app.intercom.io"] move scratchpad; [instance="slack"] move scratchpad; [instance="spotify"] move scratchpad; [instance="sublime_text"] move scratchpad;

# swap caps lock and escape
exec_always --no-startup-id xmodmap -e "clear lock"
exec_always --no-startup-id xmodmap -e "keysym Caps_Lock = Escape"
exec_always --no-startup-id xmodmap -e "keysym Escape = Caps_Lock"
#exec_always --no-startup-id xmodmap -e "add Lock = Caps_Lock" 
# Touchpad settings
exec_always --no-startup-id xinput --set-prop "SynPS/2 Synaptics TouchPad" "libinput Natural Scrolling Enabled" 1 
exec_always --no-startup-id xinput --set-prop "SynPS/2 Synaptics TouchPad" "libinput Click Method Enabled" 1
exec_always --no-startup-id xinput --set-prop "SynPS/2 Synaptics TouchPad" "libinput Tapping Enabled" 1
exec_always --no-startup-id xinput --set-prop "SynPS/2 Synaptics TouchPad" "libinput Accel Speed" 1
exec_always --no-startup-id xinput --set-prop "pointer:Microsoft Microsoft® SideWinder™ X3 Mouse" 288 1

# Screen shot
bindsym $mod+w exec flameshot gui
# Reconfigure monitors
bindsym $mod+Shift+m exec xrandr --auto && monitors

#TODO: Try a later version of i3 to get this working, current version is from 2017
# [workspace=$ws1] move workspace to output eDP-1

# Compton
exec_always --no-startup-id compton
exec_always --no-startup-id hsetroot -solid "#000000"
